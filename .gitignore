node_modules
dist

# Windows: accidental file when shell output is redirected to `NUL` in-repo
NUL
*.local
.DS_Store

# Proprietary Chip's Challenge (MS) — place zip/DAT/EXE here; never commit
vendor/
chips_challenge.zip
_zip_preview/

# Generated from proprietary assets (reproducible via npm run ms:extract)
output/

# Game pack output must live in chips-challenge-web (see tools/gamePackOut.mjs)
public/
